use std::path::PathBuf;

// ICI: FOCNTION QUi TRI ET INDEX UN FICHIER!

pub fn hsort(
  input: PathBuf,
  iRA: usize,
  iDec: usize,
  output: PathBuf,
  tmp: Option<PathBuf>,
) -> Result<(), Box<dyn Error>> {
  toto
}

pub fn hsort_file(
  input: PathBuf,
  iRA: usize,
  iDec: usize,
  output: PathBuf,
  tmp: Option<PathBuf>,
) -> Result<(), Box<dyn Error>> {
  toto
}

pub fn hsort_files(
  input: PathBuf,
  iRA: usize,
  iDec: usize,
  output: PathBuf,
  tmp: Option<PathBuf>,
) -> Result<(), Box<dyn Error>> {
  // check header compatibilty
  // Only the first BINTABLE HDU, ...
}

// Make an iteartor from one file, and, from it...
// Build an iterator from several files...
